<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:WorkHub.ViewModels"
             x:Class="WorkHub.Views.ChangePasswordPage"
             x:DataType="vm:ChangePasswordViewModel"
             Title="Change Password">

    <ScrollView>
        <VerticalStackLayout Spacing="16" Padding="24" HorizontalOptions="Fill">
            <Entry Placeholder="Current Password" Text="{Binding CurrentPassword}" IsPassword="True" ReturnType="Next" />
            <Entry Placeholder="New Password (min 8 characters)" Text="{Binding NewPassword}" IsPassword="True" ReturnType="Next" />
            <Entry Placeholder="Confirm New Password" Text="{Binding ConfirmPassword}" IsPassword="True" ReturnType="Done" />

            <Label Text="{Binding ErrorMessage}" TextColor="Red" IsVisible="{Binding HasError}" />
            <Label Text="{Binding SuccessMessage}" TextColor="Green"
                   IsVisible="{Binding SuccessMessage, Converter={StaticResource StringNotEmptyConverter}}" />

            <Button Text="Change Password" Command="{Binding ChangePasswordCommand}"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}" />

            <ActivityIndicator IsRunning="{Binding IsLoading}" IsVisible="{Binding IsLoading}" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>

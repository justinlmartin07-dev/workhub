<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:shapes="clr-namespace:Microsoft.Maui.Controls.Shapes;assembly=Microsoft.Maui.Controls"
             xmlns:controls="clr-namespace:WorkHub.Controls"
             x:Class="WorkHub.Controls.BackButton">

    <Border StrokeThickness="0" BackgroundColor="Transparent"
            WidthRequest="40" HeightRequest="40"
            StrokeShape="RoundRectangle 20"
            controls:TapFeedback.Enable="True">
        <Border.GestureRecognizers>
            <TapGestureRecognizer Tapped="OnTapped" />
        </Border.GestureRecognizers>
        <shapes:Path Data="M 20 12 H 4 M 4 12 L 10 18 M 4 12 L 10 6"
                     Stroke="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}"
                     StrokeThickness="2"
                     StrokeLineCap="Round"
                     StrokeLineJoin="Round"
                     Aspect="Uniform"
                     WidthRequest="16" HeightRequest="16"
                     HorizontalOptions="Center" VerticalOptions="Center" />
    </Border>
</ContentView>

<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="WorkHub.Controls.UserAvatarView">

    <Border x:Name="AvatarBorder" WidthRequest="36" HeightRequest="36"
            StrokeShape="RoundRectangle 18"
            StrokeThickness="0"
            BackgroundColor="{AppThemeBinding Light={StaticResource PrimaryLight}, Dark={StaticResource PrimaryMuted}}">
        <Border.GestureRecognizers>
            <TapGestureRecognizer Tapped="OnTapped" />
        </Border.GestureRecognizers>
        <Grid>
            <Label x:Name="InitialsLabel"
                   HorizontalTextAlignment="Center" VerticalTextAlignment="Center"
                   FontSize="13" FontAttributes="Bold"
                   TextColor="{AppThemeBinding Light={StaticResource PrimaryMuted}, Dark={StaticResource PrimaryLight}}" />
            <Image x:Name="ProfileImage"
                   Aspect="AspectFill"
                   IsVisible="False" />
        </Grid>
    </Border>
</ContentView>
